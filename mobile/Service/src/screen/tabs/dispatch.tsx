import React, {FC} from 'react';
import {Text} from 'react-native';

interface IProps {}

const DispatchScreen: FC<IProps> = ({}) => {
  return <Text>Dispatch Screen</Text>;
};

export default DispatchScreen;
